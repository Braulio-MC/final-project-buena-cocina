<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.BuenaCocina" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowEnterTransition">@transition/explode</item>
        <item name="android:windowExitTransition">@transition/explode</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_splash_screen</item>
        <item name="postSplashScreenTheme">@style/Theme.BuenaCocina</item>
    </style>
</resources>